# OAuth2 user credentials JSON (refresh_token + client_id + client_secret)
# OR service account JSON — paste the full JSON on one line, or use a file path.
GOOGLE_CALENDAR_CREDENTIALS=

# Optional default calendar (defaults to "primary")
# GOOGLE_CALENDAR_ID=primary
