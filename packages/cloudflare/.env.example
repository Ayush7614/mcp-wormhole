CLOUDFLARE_API_TOKEN=your_api_token_here
# Optional — default account for Workers API when account_id is omitted
# CLOUDFLARE_ACCOUNT_ID=
# Optional — default zone for DNS/cache/firewall when zone_id is omitted
# CLOUDFLARE_ZONE_ID=
