# @mcp-wormhole/template

Copy this directory when adding a new MCP server:

```bash
cp -r packages/_template packages/asana
# then edit package.json name, implement src/index.ts + client.ts
```

Do not publish this package — it is a scaffold only.
